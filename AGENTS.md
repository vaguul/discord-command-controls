# Agent Guide

- Keep this package focused on reusable Discord command metadata and policy helpers.
- Do not add Discord tokens, private guild IDs, webhook URLs, customer data, or dashboard secrets.
- Keep command catalog entries, policy targets, and tests in sync.
- Update README, CONTRIBUTING, and workflow validation when public behavior changes.
- Run `npm run typecheck` and `npm test` before committing.
